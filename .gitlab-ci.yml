image: aergus/latex

build:
  script:
    - cd src
    - pdflatex -interaction=nonstopmode -synctex=1 -output-format=pdf -output-directory=../out containerization.tex
  artifacts:
    paths:
      - "./out/containerization.pdf"
